spring.application.name=aircraft-mysql

# app 실행때마다 db 초기화
spring.sql.init.mode=always
# entity 클래스가 테이블 자동생성 비활성화
spring.jpa.hibernate.ddl-auto=none

# datasource 설정
# MYSQL DB쓴다는 것을 지정 -> schema.sql & data.sql 대신 schema-mysql.sql과 data-mysql.sql을 찾아서 사용
# spring.sql.init.platform=mysql

# spring.jpa.hibernate.ddl-auto=update

# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

spring.datasource.url=jdbc:mysql://localhost:3306/nareun
spring.datasource.username=root
spring.datasource.password=Tjdgh@130